name: 'Check spelling'
on: # rebuild any PRs and main branch changes
  pull_request:
  push:

jobs:
  spellcheck: # run the action
    runs-on: ubuntu-latest
    steps:
    - name: Spell Check Repo
      uses: actions/checkout@v7
      uses: crate-ci/typos@v1  # or v1.50.2
