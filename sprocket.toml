[check]
all_lint_rules = true
except = ["ContainerUri"]
deny_notes = true

[format]
sort_inputs = true

[run.task]
cpu_limit_behavior = "try_with_max"
memory_limit_behavior = "try_with_max"

[test]
parallelism = 50
throttle = 50

